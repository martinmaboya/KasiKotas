# Server Port
server.port=${PORT:8080}

# Database connection (PostgreSQL)
spring.datasource.url=jdbc:postgresql://dpg-d1p1cl3uibrs73d8s5e0-a.oregon-postgres.render.com:5432/kasikotas_db_si2n
spring.datasource.username=kasikotas_db_si2n_user
spring.datasource.password=rZUcjAFMOwF4ZXAPDrhsobFvAqCdSm5E
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.pool-name=KasiKotasHikariPool

# JPA and Hibernate Configuration for PostgreSQL (OPTIMIZED)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.generate_statistics=false

# Spring Mail Configuration for Yahoo (OTP Password Reset)
# Using port 465 with SSL (port 587 blocked by Render.com)
spring.mail.host=smtp.mail.yahoo.com
spring.mail.port=465
spring.mail.username=m.maboya@yahoo.com
spring.mail.password=xicrwhfsdvoltvrv
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.mail.yahoo.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.test-connection=false

# Admin Email Address
admin.email=m.maboya@yahoo.com
jwt.secret=r1QycRsGXwPCyZxDTw9vIiCIV8ADcvQRxPXe/pe7cPcJv0oHH8iOVTHA9WjldrzKDu2x1OaCMjhQYbzdtlqsQQ==

# Jackson Configuration for proper date/time handling
spring.jackson.deserialization.adjust-dates-to-context-time-zone=false
spring.jackson.serialization.write-dates-as-timestamps=false

# Cache Configuration (OPTIMIZED)
# Increased size from 1000 to 2000 entries
# Increased TTL from 30s to 60s for better cache hit rate
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=2000,expireAfterWrite=60s

# Bank Details Security Encryption (AES-256)
# Prefer a Base64-encoded 256-bit key (44 characters).
# IMPORTANT: This should be set via environment variable in production.
# For development only, generate a new key with: BankDetailsEncryption.generateNewEncryptionKey()
app.security.bank-encryption-key=${BANK_ENCRYPTION_KEY:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}

# WebAuthn / Passkey configuration
webauthn.rp-id=${WEBAUTHN_RP_ID:kasikotas-frondend.onrender.com}
webauthn.rp-name=${WEBAUTHN_RP_NAME:KasiKotas}
webauthn.origin=${WEBAUTHN_ORIGIN:https://kasikotas-frondend.onrender.com}
webauthn.challenge.ttlMs=${WEBAUTHN_CHALLENGE_TTL_MS:300000}
webauthn.allowed-origins=${WEBAUTHN_ALLOWED_ORIGINS:https://kasikotas-frondend.onrender.com,http://localhost:5173,http://localhost:5174,http://localhost:3000}

# ===== PERFORMANCE OPTIMIZATIONS (ADDED) =====

# Response Compression (60-80% size reduction)
server.compression.enabled=true
server.compression.min-response-size=1024
server.compression.excluded-mime-types=image/png,image/jpeg,image/gif,image/webp

# Tomcat Thread Pool Optimization
server.tomcat.max-threads=250
server.tomcat.min-spare-threads=10
server.tomcat.accept-count=100

# Keep-alive ping URL (prevents Render free tier cold starts)
app.base-url=${APP_BASE_URL:}

# Logging Optimization (reduce I/O)
# Keep default logging - custom levels removed for stability
logging.level.root=INFO
